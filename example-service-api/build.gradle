description = 'Example OSGi service API'

jar {
    manifest {
        instructionReplace 'Bundle-Name', "$project.description"
    }
}

// publish API to Maven
publishing {
    publications {
        api(MavenPublication) {
            artifactId 'osgi-example-service-api'
            from components.java
        }
    }
}
